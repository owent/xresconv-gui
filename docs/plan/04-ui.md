# 04 UI、交互与前端迁移

[执行索引](README.md) · [上一册](03-domain-conversion.md) · [下一册](05-packaging-release.md)

对应 P4。采用主计划确定的 React、TypeScript、Vite、React Aria Components、Zustand、CSS Modules；这是桌面本地 SPA，不引入 SSR 或常驻前端服务器。

截至 P4-07 的一致性约束：加载成功后才更换配置路径；加载互斥，旧会话的排队操作/响应不能覆盖新配置或故障状态；树版本跨 reload 单调推进；活动运行必须完成日志收尾后才接纳下一次 run。设置输入/输出为独立快照，矩阵重估释放旧限制，preview 按 `(outputDir, rename)` 二元组分组。运行控制（P4-06）：开始/取消/重置门禁由后端状态机决定（run 允许自 ready/终态、cancel 仅活动运行且重复幂等、reset 先取消等清理再重武装）；运行终态文案区分实际阶段与已发生副作用（before/计划构建失败未启动转换、after 失败如实说明输出已生成、stdin 批次失败不伪造条目级明细），事件缺失时退化为通用文案。日志（P4-07）：UI 有界窗口（2000）按 seq 与 getLogs/事件流幂等对齐，beforeSeq 向后分页；筛选只影响显示与复制/导出；富文本仅 ANSI SGR 固定色表（无 HTML 通道），导出经原生 save 对话框 + 壳层唯一写盘命令。长期 guardian 健康视图使用监督状态，不套用 one-shot NodeHealth。回归见 [增量审查](records/REVIEW-P2-P5-2026-09-24.md)。

## 页面和组件边界

| 区域/拟组件 | 展示与操作 | 数据来源 | 关键验收 |
| --- | --- | --- | --- |
| AppShell / EnvironmentDiagnostics | 环境诊断（版本/启动参数/健康/Java）写入运行日志；无独立状态条 | 壳桥接 + backend checkJava | Java 缺失仍能打开 GUI；诊断在日志可查、不占主面板 |
| ConversionTree / TreeToolbar | 分类、三态勾选、禁用、展开、全选/全不选、搜索 | 树快照、选择版本、UI 展开状态 | 勾选和焦点分离，父子级联与旧版一致 |
| ItemDetails | 名称、说明、来源、scheme、tag/class | 当前聚焦 item | 聚焦变化不偷偷改变转换选择 |
| ConversionSettings | work_dir、JAR、协议文件、数据目录、版本、参数 | 配置 + 受控 override | 多值、空值、路径校验不被单行表单压扁 |
| OutputMatrixEditor | 格式、rename、output_dir、tag/class 限定 | 领域矩阵快照 | 多输出组合、未知类型、禁用理由可见 |
| HookControls | before/after/log 事件启用、描述、mutable | hook 定义与可编辑状态 | immutable 开关不可改；被禁用事件不执行 |
| CustomActionBar | 选择器及 reload/select_all/script 等动作 | 配置按钮定义 | 原 action 顺序与名称、样式语义保留 |
| RunControls / RunSummary | 预览、执行、取消、重置、成功/失败/后处理状态 | 后端 RunContext | 只显示有证据的进度和结果，不乐观报完成 |
| LogPanel / DialogHost | 虚拟日志、筛选/导出、用户脚本弹框 | 日志游标及 callbackToken | 回调顺序、焦点恢复、安全富文本和原日志可查 |

四轮修复（2026-09-26 深夜反馈 12 项，全部落地并经几何探针/计算样式/axe/真实配置 E2E 机检验证；视觉分析服务当日不可用，截图存档 build/）：

- **面板重叠/对齐（探针实证根因）**：全站缺 `box-sizing: border-box`——`width:100%` 的左树面板把 padding+border 挤出 grid 轨道（实测超宽 26px、与右栏重叠 14px、树底溢出视口 2px；设置弹窗字体/字号输入重叠 10px 同因）。全局 border-box 后：左右净间隙 12px、底边差 0（对齐）、弹窗输入不再叠。
- **按钮样式系统性失效（本轮最大发现）**：RAC 传自定义 className 会替换默认 `react-aria-Button` 类（与三轮复选框同坑）——`.react-aria-Button.btn-primary` 等复合选择器自二轮起从未生效，主操作按钮一直是浏览器原生灰（用户历轮"按钮不明显"的真正根因）。按钮基样式改为元素级 `button{}`+变体直击 `.btn-*`；"开始转换"（accent 蓝、加粗、固定最右）与"重置"（success 绿、加粗）行尾成组；亮色 accent 加深至 #1f57e8（原 #2f6bff+白字 4.5:1 临界不过 axe），禁用态改专用 `--color-disabled-ink`（axe 零违例）。
- **树层级**：虚拟化树 DOM 实测平铺（无嵌套），CSS 后代缩进不可行——渲染层传 `level` 内联缩进（18px/级，实测 69→91→109px 逐级内进）；分类行加粗（600/400）；勾选符号改标准 `✓`/`–` 字形（原 border 旋转画法观感怪异）。
- **预览只进运行日志**：内联预览面板删除，摘要/冲突/任务清单（前 50 条）全部为日志行；失败也记日志。
- **输出冲突误报根除（xresconv.rel.xml 实证）**：原判定"同 (outputDir,rename) 组内任务数>1"把一切多条目配置都判为冲突。真实语义：最终文件名由 xresloader 从各条目 scheme 的 OutputFile 取（SchemeConf.getOutputFile 源码核验），GUI 无法跨条目判重——只有"同一条目以相同 (type,outputDir,rename) 被重复发射"才是可证明的冲突。实测该配置 37 条目 64 任务：其余 33 条目不再误报，报告的 4 个条目（局内掉落表/掉落组随机/掉落包跳转/随机池）经查在 xresconv.xml 中确实各定义了两次（真实重复转换）。
- **详情弹窗去重复+重排**：重命名全 GUI 唯一入口=输出矩阵（datalist 可选可输、预设按矩阵源后缀推导）；弹窗加宽至 min(72rem,94vw)，路径/多值字段通栏等宽，短字段（数据版本/协议/输出类型）标签与控件同行。
- **重置门禁对齐后端**：backend `session.reset()` 无状态门禁（终态重新武装、ready 幂等清理）；此前前端比后端更严致按钮在 ready 态长期灰置。现除 loading 外可点。
- **日志三项**：加载配置/开始转换成功即重置显示面（清窗口、initialized 防重拉、保留 seq 水位防旧事件灌回）；「运行日志」标题加粗加大（700/lg）仍与工具栏同排；滚动模式行改 `pre` 不折行（横向滚动条实测出现，scrollW 8110>1086），换行模式接 virtualizer `measureElement` 动态测高（实测行高 22/44/176 共存、零溢出重叠）。

三轮修复（2026-09-26 晚间反馈 7 项，全部落地并经单测/浏览器三引擎/真实 WebView E2E/浏览器探针验证）：

- **启动报错根除（BACKEND_NOT_READY）**：三个叠加根因全部修复——① `useDisplaySettings` 的自动加载受 StrictMode 双挂载取消（cancelled 标志吞掉整个引导，dev 下主题/自动加载从未生效）→ 改为模块级单例 store（useSyncExternalStore 订阅，多组件消费同一状态）；② `initLogs` 撞上启动瞬态错误即置 `initialized` 并写 lastError，错误永久卡在树上且日志再拉不出 → 瞬态错误（BACKEND_NOT_READY/BACKEND_TIMEOUT/guardian/channel，共享 `startup-retry.ts` 判定）静默重试（600ms×50≈30s），非瞬态仍立即可见；③ `initLogs` 成功后整替 entries 会清掉重试窗口内的本地 `[GUI]` 行 → 改合并（seq 去重，历史页在前）。自动加载重试期间清掉瞬态 lastError 并在日志记一条"后端启动中"提示；超时才升级为可见错误。
- **复选框/单选钮可见化**：RAC 1.x 把原生 input 包进 `VisuallyHidden`，且自定义 `className` 会**替换**默认 `react-aria-Checkbox/Radio` 类——可见方框/圆点必须是真实子元素（树与事件开关的 `.checkbox-mark`）或直击自定义类的 CSS；树复选框三态（对勾/横杠/空）+ 主题分段单选（圆点指示器）均可视、可点（真实 WebView E2E 点击 `.tree-checkbox` 切换勾选通过）。
- **状态条移除**：顶部 EnvironmentStatus 整条删除（版本/健康/Java/启动参数/设置按钮行）；调试信息全部写入运行日志（`environment-diagnostics.ts`，`[GUI]` 前缀：版本/协议、启动参数、guardian+backend 健康、Java 检查结果与推荐发行版）；supervisor ready/died 事件后重查并记录状态迁移。
- **显示设置入口与弹窗**：⚙ 按钮移至文件行"详情…"旁（同排不另起行）；弹窗分区（主题/分区字体/上次转换列表），主题为分段单选（此前圆点不可见="没有任何 UI 控件"的根因），字体区带表头网格。
- **详情弹窗重排**：分区标题（转表工具与目录/数据与协议/输出重命名/条目详情/输出矩阵）+ 两列网格 + 路径与多值字段通栏等宽 + 底部固定操作行（提示+关闭）。
- **暗色对比度**：`[data-theme="dark"]` 补 `color-scheme: dark`（原生表单控件/滚动条随主题）；状态章改实底语义色+专用墨色 token（axe AA 验证）；"开始转换"加大加粗（`.btn-run`）。

二轮修复（2026-09-26 深度反馈 13 项，全部落地并真实 E2E 验证）：

- **启动时序**：自动加载（CLI --input 优先 → 显示设置上次文件）带 backend 就绪重试（600ms 轮询/30s 上限，BACKEND_NOT_READY 类可重试，业务失败不重试）——消灭启动 "guardian protocol violation"。
- **树交互**：点行/点标题即切换勾选（fancytree 点行语义恢复）；标题超长省略号（title 提示保留）；树面板撑满左列；真实 WebView E2E（`tree-select.spec.mjs`，经 --input 注入配置）验证勾选/反选/高度/省略/页面内容。
- **详情弹窗**：输出重命名/协议类型/输出类型移入详情弹窗（主页面只留文件行+并发数+详情按钮）；重命名改原生 input+datalist——焦点保持、可连续输入、预设按已输入过滤、源后缀按配置矩阵推导。
- **显示设置弹窗**：入口=文件行"详情…"旁 ⚙ 按钮（同排）；四区字体字号设置（全局/界面控件/树/日志；family 经 queryLocalFonts 枚举+datalist 输入过滤，size 白名单 6–48px；CSS 变量 `--font-<area>-*` 应用；Rust 白名单校验+控制字符清洗）。
- **日志**：预览结果（任务数/目录/JAR/冲突明细）写入运行日志（本地证据行 `[GUI]`）；标题与级别/筛选/按钮同一行紧凑排布（非粗、非标题字体）。
- **主题强化**：主按钮彩色（转换列表文件/开始转换=accent 主色，重置=success，详情=accent-soft）；表单控件统一现代化（hover/focus/圆角）；两主题下均明显。
- **Java 运行时检查（F06/F12 恢复旧版 conv_env_check）**：backend `checkJava` RPC（java -version 探测+版本≥8+64-Bit 判定+下载指引，契约枚举扩展）；解析链 `XRESCONV_JAVA → JAVA_HOME/bin/java → PATH`，**与 java-runner 转换执行共用同一解析**（显示=执行）；结果写入运行日志（来源标注+版本首行，不满足时 warning+推荐发行版，三轮改版起不再占主界面横幅）。

UI 改版（2026-09-26 用户反馈六项，全部落地）：

- **现代化视觉**：令牌体系升级（软阴影/圆角/hover 过渡/主按钮体系/语义软底色）；
- **自适应**：自定义按钮与转换事件在未定义时不渲染（对照旧版 conv_list_event_group_wrapper 默认隐藏）；
- **详情弹窗**：“详情…”按钮承载原详细配置全部字段（六字段+条目详情+输出矩阵，打开即取当前 effective——加载后自动刷新）；重命名改为 ComboBox 单输入：可直接输正则、下拉给预设、输入即时过滤；预设源后缀按当前配置 outputMatrix 推导（未加载回退 .bin，目标侧保持旧版五预设）；
- **显示设置**：主题三态（跟随系统/亮/暗，`<html data-theme>`，tokens.css 三态）+ 上次转换列表自动加载；持久化到可执行程序目录 `display-settings.json`（壳层 read/write_display_settings 命令，白名单校验）；入口=文件行 ⚙ 按钮；
- **默认字体栈（2026-09-26 用户指定，未设置字体时生效）**：UI=更纱黑体 UI SC→Sarasa UI SC→等距更纱黑体 SC→Sarasa Mono SC/CL→Noto Sans Mono(→CJK SC)→Maple Mono Normal NF CN→微软雅黑→系统默认；树=Maple Mono Normal/NF NF CN→更纱黑体 SC/Sarasa Gothic SC→等距更纱黑体 SC→Sarasa Mono SC/CL→Noto Sans Mono CJK SC→Noto Sans Mono→微软雅黑→系统默认；日志=Maple Mono Normal NL/NL NF CN→Maple Mono NL CN→等距更纱黑体 SC→Sarasa Mono SC/CL→Noto Sans Mono(→CJK SC)→Maple Mono Normal/NF NF CN→微软雅黑→系统默认等宽。浏览器按序自动选用本机已有字体（不下载）；用户设置字体时经 inline `--font-<area>-family` 覆盖整栈。
- **日志溢出**：工具栏“横向滚动/换行”切换（默认滚动，换行模式行高按 2 行估算）；
- **紧凑状态行**：状态色章（success/danger/warning/info/muted 软底）与运行结果同行展示。

布局反馈（2026-09-25 用户）：转换参数区不得挤压按钮/转换列表/输出日志。落地：右侧主区自身滚动（min-height:0 + overflow-y），条目详情/输出矩阵/转换事件三个次要面板以原生 `details` 默认折叠（对照旧版事件开关组默认隐藏），参数表单紧凑化（列宽 11rem、间距收敛、多行字段 2.4rem 起步），底部区域固定 `clamp(11rem, 26vh, 34vh)` 高度优先日志。折叠面板仍可键盘展开，F 功能入口无删减。

所有原入口保留。新增搜索、筛选和取消属于辅助操作，不改变“全选”的原含义；默认全选针对整个有效配置，若增加“仅选搜索结果”必须独立命名。日志筛选不影响落盘日志和脚本 hook。

## 状态分层

- Node.js backend：配置 revision、真实选择、参数 override、转换 plan/run 和事件开关；guardian 提供实际进程状态与清理结果。
- Tauri 壳：窗口、文件对话框、限定权限桥接及后端连接状态，不维护第二份领域状态。
- Zustand：后端快照缓存、事件水位，以及面板宽度、主题、搜索词、焦点、滚动位置等 UI 状态。
- 组件本地：尚未提交的表单输入、校验提示和控件展开状态。

前端从 adapter 经 Tauri 桥接获取 Node.js 数据；组件不得自行 `spawn`、读 XML 或拼 Java 命令。表单提交必须确认后端版本，版本冲突先重同步，不能覆盖用户更新。选择操作可采用带 requestId 的临时显示，但失败时必须回退并解释；业务启动只读后端已确认状态。

React StrictMode 下重复挂载不能建立重复事件订阅或启动任务。所有监听有释放句柄；异步回复使用 session/revision/runId 校验。不要在 useEffect 中以“状态变为 ready”为触发自动执行转换。

backend 失联时，壳与 UI 仍可显示故障、查询 guardian 清理状态并关闭窗口；guardian 失联时由壳显示自身可确认的状态，不能依赖失效 backend 返回错误。重新启动服务后先恢复或明确结束旧会话，不自动重放转换；旧缓存标记失效，不能继续显示运行成功。

## 完整交互路径

1. 启动后查询快照和环境；处理旧 CLI 参数或显示打开配置入口。
2. 打开/重载时展示进度与可取消状态，仍保留上次有效配置；错误定位到文件/字段。
3. 配置提交后还原允许保留的 UI 状态，执行已定义的默认选择规则，显示可转换数量与限制。
4. 用户编辑参数和输出格式，后端生成预览；指出 JAR 缺失、路径不可表达、输出冲突等阻塞。
5. 开始后显示 before、Java 批次、after 和收尾的实际阶段；按钮可用性由领域状态决定。
6. 取消/重置先等待后端清理；失败仍保留日志和已有产物信息。关闭窗口时明确当前运行的停止策略。
7. 自定义脚本弹框保持 yes/no/on_close 语义；被终止 worker 的弹框自动失效，不能点击旧回调。

## 树、可访问性和虚拟化

先实现旧三态选择算法与组件适配，再启用树虚拟化。React Aria 的默认 selection 不等于 Fancytree `selectMode: 3`；父节点包含禁用叶子时的聚合值、toggle 和 getSelectedNodes 结果必须单独对照。

树优先验证 React Aria 自身树/虚拟化能力；TanStack Virtual 用于日志。若组合影响焦点、读屏或节点身份，先修适配或调整组合，不回引 Fancytree。组件源码与官方用例作为能力依据，运行效果仍由 UI03 验证。[React Aria Tree 源码](https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/src/Tree.tsx)、[官方用例](https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/stories/Tree.stories.tsx)

必测键盘：Tab/Shift+Tab、上下/左右、Home/End、Space 切换、Enter 动作、Escape 弹框、双击旧行为。不能用列表项索引作为节点 key。虚拟节点卸载后选中数据仍保留；切换焦点到未渲染节点先滚动再聚焦。

高 DPI、125%/150%/200% 缩放、中文长文本、不同系统字体、明暗主题、窗口缩小与多显示器移动都要测试。100k 节点不直接向 DOM 填充全部节点；后端快照分块与前端增量构建也计入响应预算。

## 样式、富文本与权限

CSS 自定义属性定义颜色/间距/字号/层级；尽量使用最低 WebView 已验证的能力。Vite 的 JS target 与 CSS 兼容不是同一个开关，需分别列最低浏览器特性并检查。运行依赖、字体和图标随包，本地 UI 不依赖 CDN。

旧按钮 `style`、日志 `alert-*` 映射到本地语义样式；未知值给兼容提示和可读默认，不悄悄隐藏按钮。富文本由白名单转换成 React 节点或经审计的清洗层；不要直接把脚本内容交给 `dangerouslySetInnerHTML`。必要的允许标签、颜色和链接用样例固定。

正式 CSP 约束脚本、资源与连接来源；任意用户 JS 只在 Node worker 中执行。外部链接经 URL 协议验证后由限定 opener 打开。普通浏览器预览 adapter 不含真实系统权限。[Tauri CSP](https://v2.tauri.app/security/csp/)

## P4 任务清单

| 任务 | 依赖 | 拟实现位置/动作 | 完成断言 |
| --- | --- | --- | --- |
| P4-01 | G1、接口合同 | `apps/desktop/src/app`、tokens、页面骨架 | UI01；所有 F 功能能映射到明确区域 |
| P4-02 | P1-06、P3 状态接口 | `adapters/tauri`、store、订阅释放和重同步 | UI02/SC11；backend/guardian 故障可见，断连/迟到事件/重复挂载无重复任务 |
| P4-03 | P3-04、P4-02 | 树与选择适配、键盘基础行为 | CF04/UI03；选中集合、禁用与三态对照通过 |
| P4-04 | P3-05、P4-02 | 表单、详情、输出矩阵与预览 | UI04；字段/多值/阻塞可见，版本冲突不覆盖 |
| P4-05 | P2-06、P4-02 | 按钮动作、hook 开关、DialogHost | SC06/UI05；不把回调简化成普通 toast |
| P4-06 | P3-08、P4-02 | RunControls、阶段与取消/重置 | EX03/UI06；后处理失败和未知条目结果表达准确 |
| P4-07 | P3-09、P4-02 | 日志分页、筛选、复制/导出、富文本 | EX04/UI07；无 XSS、无业务日志静默丢失 |
| P4-08 | P4-03 至 P4-07 | 虚拟化、主题、DPI、读屏和三引擎测试 | UI03/UI08；性能与可访问性共同通过 |
| P4-09 | P4-08 | 新依赖/产物扫描、真实桌面 E2E、G4 报告 | 无 jQuery/Bootstrap/Fancytree/Popper 运行依赖；旧实现仅供对照 |

UI 尚未完成时可以保留旧架构启动入口，不能靠在 React 中嵌套旧 jQuery 页面完成验收。视觉调整不应改变配置字段和脚本的业务语义。
