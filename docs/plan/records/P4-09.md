# P4-09 依赖/产物扫描、真实桌面 E2E 与 G4 报告

- task_id: P4-09
- status: done
- owner: main agent
- source_commit: 工作树（接续 P4-08）
- input_versions: Node v24.21.0、Yarn 4.18.0、WebView2/msedgedriver 153.0.4234.48、tauri-driver 2.0.6、@wdio/* 9.32.0
- commands: 见文末"验证命令与实测计数"
- cwd: D:\workspace\projs\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `apps/desktop/test/deps-scan.test.ts`（新）、`tests/desktop/specs/p4-ui.spec.mjs`（新）、`biome.json`（test-results 排除）、本文件
- rollback_point: 三个新增文件 + biome includes 一行均可逐文件恢复

范围说明：04-ui.md P4-09 行（新依赖/产物扫描、真实桌面 E2E、G4 报告）。E2E 驱动环境重建：本机 tauri-driver（cargo install 2.0.6）与 msedgedriver 153.0.4234.48（npmmirror 镜像，`build/tools/`，与 WebView2 运行时精确匹配；`MSEDGEDRIVER_PATH`/`TAURI_DRIVER_PATH` 注入，路径须为 Windows 风格——POSIX 风格 env 值经 Node existsSync 失败，实测定位）。

## 实现与证据

### 1. 依赖/产物扫描（G4："无 jQuery/Bootstrap/Fancytree/Popper 运行依赖"）

`deps-scan.test.ts` 三例：

- 工作区（apps/*、packages/*）生产依赖白名单扫描——jquery/jquery.fancytree/bootstrap/@popperjs 均未出现（旧依赖仅存在于仓库根 package.json，P7 删除）。
- 新架构源码（apps/desktop/src、packages、src-tauri/src）import/require/@import 模式扫描——零命中。
- 生产构建产物（apps/desktop/dist，存在时）代码级特征（fn.jquery/fancytree/data-bs-/popper）扫描——零命中；注释/许可文本误报不计。

### 2. 真实桌面 E2E 扩展（真实 WebView2 153）

新增 `p4-ui.spec.mjs` 6 例（合计 launch 3 + p4-ui 6 = 9/9 通过）：

- 运行控制区：未加载配置时四按钮禁用 + 状态行"未加载配置"（UI06 门禁真实路径）。
- 日志面板：筛选/复制/导出控件渲染 + 窗口信息"完整日志见磁盘"（UI07）。
- 文本筛选输入/清空保持空态；无日志复制 → 可读错误（role=alert），不崩溃。
- 窗口缩至 900×600 无整页横向溢出（UI08 布局）。
- tokens.css 暗色 prefers-color-scheme 块存在且 --color-bg 可计算（UI08 主题，真实 WebView 计算）。

### 3. G4 报告

| G4 出口条件 | 证据 | 结论 |
| --- | --- | --- |
| 浏览器测试全通过 | Playwright 三引擎（chromium/firefox/webkit）15/15（P4-08） | 本机 win32 通过 |
| 真实桌面 E2E 全通过 | WebView2 153 真实应用 9/9（含壳→guardian→backend 握手链） | Windows 通过；Linux/macOS 待 CI（与 G1/G3 同口径） |
| F01–F12 入口 | feature-map.ts 区域映射 + UI01（P4-01）；F 逐项行为证据散布 P4-03～P4-07 记录 | 通过（对照表见各记录验收映射） |
| 键盘与大数据 | 树键盘单测（P4-03）+ 10k/100k 窗口化（P4-08）+ 100k 解析/序列化/传输（P4-08） | 通过；真实 WebView 键盘全键位走查随 P6/平台矩阵 |
| 界面与业务状态无双重真相 | 状态权威在 backend（快照+事件），UI 仅缓存与 UI 态（04-ui §状态分层；P4-02/P4-06/P4-07 记录） | 通过 |
| 无旧 UI 运行依赖 | deps-scan 3 例 | 通过 |

**G4 判定：Windows 实测范围通过**；Linux/macOS 真实桌面、三引擎跨平台、真实 p95 性能阈值验收随 CI/P6（不据此宣称跨平台 UI 验收完成）。

## 验证命令与实测计数（2026-09-25 win32/x64）

```text
corepack yarn lint / typecheck    # 0 error（biome 排除 test-results 产物目录）
corepack yarn test:unit           # 597 passed + 10 条件跳过（desktop 118：+deps-scan 3）
corepack yarn test:browser        # 15 passed（三引擎）
# 驱动环境：cargo install tauri-driver --locked；msedgedriver 自
#   https://registry.npmmirror.com/-/binary/edgedriver/153.0.4234.48/（国内镜像）
MSEDGEDRIVER_PATH=build/tools/msedgedriver.exe corepack yarn test:desktop
                                  # 真实 WebView2：9/9（launch 3 + p4-ui 6）
```

## 遗留

- p95 交互实测（候选 <100ms@参考环境）未做：需真实数据加载路径（--input 注入或对话框注入适配），归 P6-05 性能测量；当前只有功能断言。
- 真实 WebView 键盘全键位（Tab/Home/End/Enter/Escape）走查随平台矩阵 E2E；单测已覆盖树键盘语义。
- 原生文件对话框真实打开/选择/取消的自动化或人工记录（06 册要求每平台至少一次）随 P5-10/P6-04。
- E2E 驱动（tauri-driver/msedgedriver）不进仓库（build/tools 已 gitignore）；CI 侧安装步骤归 CI-03（P5 CI 任务）。
