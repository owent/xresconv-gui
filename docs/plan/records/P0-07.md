# P0-07 G0 基线审阅

- task_id: P0-07
- status: done
- owner: kilo-agent
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39
- dirty_tree_digest: 工作树改动均为 P0 产物（tests/fixtures、docs/plan/records、build/p0-baseline），不改变被测旧行为
- input_versions: 同 P0-01/P0-04（Electron 41.10.3、OpenJDK 25.0.4.1、xresloader 2.23.6 主基线）
- commands: 无新执行；本任务为审阅与归档（核对 P0-01…P0-06 产物）
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `docs/plan/records/P0-01.md`…`P0-06.md`、`tests/fixtures/`、`build/p0-baseline/`
- rollback_point: 969b69850fb9970192511a0539cf268d3e7e7c39

## G0 门槛核对（门槛定义见 docs/plan/README.md「阶段总门槛」）

| 必需证据 | 产物 | 结论 |
| --- | --- | --- |
| 工作树标识 | P0-01：HEAD/baseline commit、51 文件 SHA-256 清单 | 通过 |
| 旧行为样例 | P0-02（F01–F12 fixture + manifest，观察已回填 verified）、P0-03（脚本契约）、P0-04（20+ 次真实 GUI 运行采集） | 通过 |
| 平台候选 | P0-06：D1–D5 全部决策，平台矩阵定稿（64 位 × Win/macOS/Linux，Linux 首批发行版已定） | 通过 |
| 大小/性能原始记录 | P0-05：367.66MB 解压 / 144.47MB zip、启动 ≈600–703ms、10k/100k 树、137 条/s 日志、6 任务 1510ms | 通过 |

结论：**G0 通过，允许进入 P1**。P0 出口各项（Plan.md §10 P0 清单）均有对应记录；其中"将无法兼容的 DOM/Electron/节点行为列为明确阻塞"经 D3 决策改为"不在兼容承诺内 + 运行时诊断与迁移指引"（P0-06）。

## 固定的预期结果集（不可自动重写）

以下文件是后续阶段的验收基准；任何修改必须伴随契约变更说明或显式 BD 变更，测试不得自动重写预期：

1. `tests/fixtures/scripts/contract.md` —— 五类脚本入口契约（含实测语义）。
2. `tests/fixtures/legacy/manifest.json` —— F01–F12 + 故障 fixture 清单与已验证观察。
3. `tests/fixtures/conversion/golden/`（6 个输出 + `SHA256SUMS.txt`）—— C15 字节级回归基准（固定组合：`build/p0-baseline/legacy-run/convert_list.xml` + JDK 25.0.4.1 + JAR 2.23.6，已验证两次运行逐字节一致）。
4. `tests/fixtures/config/`、`selectors/`、`scripts/`、`faults/` —— 输入与预期事件轨迹。

## 旧行为差异归类（每条观察归入契约保留或显式 BD 变更）

| 观察（P0-04 证据） | 归类 | 处理 |
| --- | --- | --- |
| 事件 data 每次新建、按钮 data 跨调用共享、on_append_log 同条日志共享 context 且逐条新建 | 契约保留 | contract.md §1/§2/§5；SC 用例 |
| alert_warning yes→on_close 顺序 | 契约保留 | contract.md §5；回调 ID 往返设计（02 分册） |
| LIFO 派发顺序、exit code 累计失败归并、命令 argv/stdin 编码形态 | 契约保留 | P0-04 命令形态记录；P3 状态机/编码器对照 |
| 事件/set_name 脚本含 `<>&` 必编译失败（HTML 解析 `.html()` 重转义）、`<script>` raw-text 特殊化、非法 XML 容错加载、CDATA 无支持 | **BD-07（新增）** | 严格 XML 解析 + 脚本文本语义统一；旧容错/转义缺陷不复制，给可定位诊断 |
| include 竞态（第二 include 条目丢失、全局值覆盖父配置）、循环 include alert 阻塞加载 | BD-05 | 快照提交 + 确定性 include 顺序；缺陷不复制 |
| alert_warning ESC 关闭无任何回调、事件挂起至超时 | BD-06 | 关闭/取消必须收尾，不允许永久挂起 |
| 异步 throw 冻结渲染、set_name 死循环冻结、process.exit 杀渲染留孤儿 | BD-01 | 独立进程 + 外部硬截止时间 |
| 日志风暴冻结（137 条/s 同步 DOM） | BD-02 | 有序异步处理 + 有界队列 + 虚拟列表 |
| `conv_env_check` java 检测 exit/stdout 竞态误报 | 缺陷修复（非兼容差异） | 新实现正确等待进程结果；不构成行为承诺 |
| JAR 2.14.1 与 main 样本不兼容 | 环境约束 | 记录支持的 JAR 清单（P3 验证），不 blanket 承诺任意版本 |
| 脚本依赖 DOM/jQuery/Electron/真实 Fancytree 内部 | D3 已定：不兼容范围 | 运行时诊断 + 迁移指引，不做透明模拟 |

已在 `docs/plan/README.md` 的 BD 表登记 BD-07。

## remaining_limits（带入 P1+，不阻塞 G0）

- 取消/重置后的 Java 孤儿进程实证、多配置会话并行：转 P6（P0-04 remaining_limits）。
- 循环 include alert 的解除机制、渲染崩溃后主进程窗口确切状态：转 P2 故障原型复核。
- `conv_env_check` 竞态的确切时序窗口未定位；新实现不复制该缺陷，无需进一步旧版取证。
- 后续阶段新发现的对象突变、模块缓存等差异须新增 BD 编号并链接复现用例（README 规则）。
