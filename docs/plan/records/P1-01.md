# P1-01 固定工具链（Node/Yarn/Rust）与可复现构建

- task_id: P1-01
- status: done
- owner: kilo-agent
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: Node v24.21.0、npm 11.16.1、Yarn 4.18.0、Rust 1.98.1（rustup 1.29.1 解析）、cargo 1.98.1
- commands: `node --version`、`corepack yarn --version`、`cargo --version`、`rustc --version`、`node scripts/check-toolchain.mjs`
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `rust-toolchain.toml`、`.yarnrc.yml`、`package.json`、`scripts/check-toolchain.mjs`
- rollback_point: 删除上述固定文件即可回到未固定状态（不推荐）

## 实施结果

- `rust-toolchain.toml`：`channel = "1.98.1"` + `rustfmt`/`clippy`/`rust-src`；`rustup show` 自动安装 1.98.1。
- `package.json`：`packageManager: "yarn@4.18.0"`（corepack 强制）、`engines: { node: ">=24" }`、`workspaces: ["apps/*", "packages/*"]`。
- `.yarnrc.yml`：`nodeLinker: node-modules`（Electron/原生工具链兼容）+ `enableScripts: true`。
- `scripts/check-toolchain.mjs`（根脚本 `check:toolchain`）：校验 node>=24、yarn=4.18.0、cargo/rustc=1.98.1、rustfmt/clippy 可用，违反即非零退出。
- 版本核验方式（2026-09-23 实测）：npm registry `npm view <name> version`、crates.io API、static.rust-lang.org channel-rust-stable.toml、nodejs.org dist index；Rust 组件版本按 rust 1.98.1 manifest 对齐（rustfmt 1.9.1-stable、clippy 0.1.98、rust-analyzer 2026-09-21）。

## 验收对照

- [x] 版本固定存在且可安装（rustup 自动解析；corepack 自动解析）
- [x] `node --version`、`yarn --version`、`cargo --version` 与计划一致（24.21.0 / 4.18.0 / 1.98.1）

## 观察

- 本机 npm 11.16.1 > 文档基线 10.9.10（Node 24.21.0 自带 11.16.x，官方站点文档滞后），以实测值为准。
- Node 26.10.0 为 Current，项目按 P0-03 定稿锁 LTS 24。

## remaining_limits

- rustup 工具链安装依赖网络；离线环境需预置 toolchain（未覆盖，CI 镜像处理）。
