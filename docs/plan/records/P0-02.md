# P0-02 旧行为 Fixture 清单（F01–F12）

- task_id: P0-02
- status: done（authored 层完成；运行观察回填属 P0-04）
- owner: kilo-agent
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39
- input_versions: 同 P0-01；外部样本 sample.xml @ sha256 66c07a0c…（7084 B，2026-09-23 抓取）
- changed_files（全部新增）:
  - `tests/fixtures/README.md`
  - `tests/fixtures/legacy/manifest.json`
  - `tests/fixtures/config/`：`README.md`、`minimal.xml`、`global-options.xml`、`tree-items.xml`、`include-parent.xml`、`include-child-a.xml`、`include-child-b.xml`、`include-cycle.xml`、`script-text-edge.xml`、`invalid.xml`
  - `tests/fixtures/selectors/`：`README.md`、`exact-match.json`、`invalid-rule.json`、`actions-chain.json`、`empty-invalid.json`
  - `tests/fixtures/scripts/`：`README.md`、`events-order.xml`、`append-log.xml`
  - `tests/fixtures/conversion/README.md`
  - `tests/fixtures/faults/`：`README.md`、`fault-sync-throw.xml`、`fault-async-throw.xml`、`fault-never-resolve.xml`、`fault-infinite-loop.xml`、`fault-process-exit.xml`、`fault-log-recursion.xml`、`fault-log-storm.xml`
- linked_requirements: F01–F12、G0 门槛
- linked_test_cases: C01–C15、R01–R09
- commands:
  - PowerShell `ConvertFrom-Json` 校验 5 个 JSON → 全部 OK
  - `System.Xml.XmlDocument.Load` 校验 18 个 XML → 15 OK；`invalid.xml`（故意非法）、`script-text-edge.xml`（故意非良构探针）按预期失败；`fault-log-storm.xml` 初版含裸 `<` 已改为 `100000 > i` 后通过
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- case_count: 0
- exit_codes: 校验全部通过
- evidence_paths: `tests/fixtures/legacy/manifest.json`（12 项功能 + 7 项故障映射，每项含来源锚点与缺口）
- remaining_limits:
  - 全部 fixture 为 `authored` 状态，`old_observation` 均 `pending-runtime`，依赖 P0-04 回填。
  - xresloader JAR 与示例表格未固定（manifest `external_artifacts` 记录为 pending-download）。
  - 每组 fixture 的覆盖缺口在 manifest 各 `gaps` 字段逐条列出（如重复 key、空属性、灾难性 regex 回溯耗时）。
- rollback_point: 969b69850fb9970192511a0539cf268d3e7e7c39
