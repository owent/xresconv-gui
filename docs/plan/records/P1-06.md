# P1-06 契约唯一源：JSON Schema → TS/Ajv（无 Cargo 依赖）

- task_id: P1-06
- status: done
- owner: kimi-code
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: Ajv 8.20.0、json-schema-to-typescript 16.0.0、TypeScript 7.0.2
- commands: `yarn workspace @xresconv/contracts generate`（两次，md5 对比）、`yarn workspace @xresconv/contracts test`、`yarn typecheck`
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `packages/contracts/schema/*.json`、`packages/contracts/src/generated/*.ts`、`packages/contracts/test/samples.test.mjs`、本文件
- rollback_point: node-health schema/样例/生成物删除即回退本任务增量

## 实施结果

- schema 目录为唯一契约源，现有 `handshake`、`error-info`、`node-health`（本轮新增，见 P1-04）三份；每份配 valid/invalid 样例，Ajv2020 strict 双向断言（6 例通过）。
- `generate`（json-schema-to-typescript）连续两次输出 md5 一致，确认生成确定性；`src/generated/` 全部机器生成，biome 排除。
- 纯 Node 链路：generate/typecheck/test 均不调用 Cargo；`build-types.mjs` 中遗留的 `generate:schema`（Cargo 导出）提示已清除，全仓 grep 无 Cargo↔schema 残留引用。
- UI/Node 角色判断一致：desktop App 直接 `import type { NodeHealth }`（生成类型），guardian 测试经 `@xresconv/contracts/schema/node-health.json` 子路径加载同一 schema 文件做运行时校验（TS 类型 ≠ 运行时校验，两者同源）。

## 验收对照

- [x] UI/各 Node 角色使用同一 schema 源（类型生成 + Ajv 运行时校验同文件）
- [x] 契约生成与测试不依赖 Cargo
- [ ] 最小 Tauri 桥接"拒绝越权/过大请求"：壳目前只做首行 JSON 解析 + 15s 截止 + 单命令白名单（invoke_handler 固定三命令），长度上限/版本拒绝策略随 P2 真实 IPC 协议落地（主计划 §4.2），本任务不提前宣称

## remaining_limits

- handshake 的 `protocol_version` 在 schema（仅约束整数 ≥1）与常量 `PROTOCOL_VERSION=1` 双处存在；bin 入口与常量的一致性由 backend 测试钉住，schema 枚举化留给 P2 协议定稿。
