# P1-03 升级旧架构到最新 Electron 并用真实基线回归

- task_id: P1-03
- status: done
- owner: kilo-agent
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: Electron 44.4.5（npm 实测 latest）、@electron/packager 20.3.0、gulp 5.0.1、jquery 4.0.0、jquery.fancytree 2.38.5、bootstrap 5.3.8、adm-zip 0.6.1、compressing 2.1.3、log4js 6.9.1、minimatch 10.2.6；基线组合 C15（`convert_list.xml` + JDK 25.0.4.1 + xresloader-2.23.6.jar）
- commands: `build/p0-baseline/run-baseline.bat -ConvertWaitMs 90000 -Scenario convert_list`（ELECTRON_RUN_AS_NODE=1 删除后执行）、`yarn run package-test`
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `build/p0-baseline/legacy-run/result-electron44.json`、`build/p0-baseline/legacy-run/evidence-electron44.log`、`tests/fixtures/conversion/golden/`（SHA256SUMS.txt）
- rollback_point: package.json 旧依赖快照在 P0-01 记录

## 实施结果

- 直接依赖全部升级到 npm 实测最新：electron ^44.4.5（升级前 41.8.2）、@electron/packager ^20.3.0（前 ^18）、gulp ^5.0.1、jquery ^4.0.0、jquery.fancytree ^2.38.5、bootstrap ^5.3.8、adm-zip ^0.6.1、compressing ^2.1.3、log4js ^6.9.1、minimatch ^10.2.6。
- **真实基线回归（Electron 44.4.5 + C15）**：start→success，两个转换节点全部成功；6 个 golden 输出 SHA256 **全部 MATCH**：
  - `backtrace_sample_loader.bin`、`backtrace_sample_loader_empty.bin`、`backtrace_sample_loader_filter.bin`（c15c0b10…）
  - `proto_kind.options.json`（3eacda79…）、`proto_kind_custom_validator.json`（260bc5be…）、`proto_kind_json.json`（ceaa11bf…）
  - 行为对照：tail-log 轮询、`no_task_running` 快速失败、名称延迟回调、postprocess 证据行均命中。
- `yarn run package-test`（@electron/packager 20）：成功产出 `out/xresconv-gui-win32-x64/`（约 1.64 min）。
- 运行前置坑（已写入脚本注释）：shell 预设 `ELECTRON_RUN_AS_NODE=1` 会静默阻止 GUI 启动，基线脚本已自动清理该变量。

## 验收对照

- [x] 旧 GUI 在最新 Electron 下真实转表，输出与 golden 完全一致（6/6）
- [x] 打包管线（packager 20）在 Windows 通过

## remaining_limits

- 仅 Windows x64 验证；Linux/macOS 打包未跑（旧架构即将退役，价值有限）。
- 旧架构回归仅作为迁移期参照，不作为 P2+ 质量门禁（门禁由 contract/unit/e2e 承担）。
