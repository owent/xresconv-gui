# P0-04 旧版行为运行采集记录

- task_id: P0-04
- status: done
- owner: kilo-agent
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（被测旧应用 = 该提交的源码 + yarn.lock 安装）
- dirty_tree_digest: 干净（工作树改动均为本次新增的 tests/fixtures、docs/plan/records，不改变被测行为）
- input_versions:
  - 应用: 2.6.0（源码运行，`node_modules/electron` 41.10.3 与 `out` 打包同源码）
  - JDK: OpenJDK 25.0.4.1 LTS x64（Microsoft）
  - JAR 主基线: xresloader-2.23.6.jar，sha256 `72fd76553ebda4257bebf196b4233a2cd630a176eace1e5467750ddd7f0caa88`，28,024,199 B
  - JAR 对照: xresloader-2.14.1.jar，sha256 `b30aaa8a65f6db3c21df002947bf46646313d10caa79af409412385f0f6b8a3d`，36,878,149 B
  - 样本: `资源转换示例.xlsx` sha256 `079c667f…559f09`（45,962 B，Git LFS）、`proto_v3/kind.pb` 7,172 B、`custom_validator.yaml` sha256 `e1085a2d…618ec`（均来自 xresloader main，2026-09-23）
  - Node（驱动器）: v24.21.0
- commands: `node build/p0-baseline/drive-gui.mjs <config> <evidence> [--no-convert|--convert-wait=N|--modal-action=yes|no|esc|--click-custom=名|--app-args=…]`（CDP 驱动真实 Electron；不模拟 DOM/Fancytree）
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- case_count: 20 次 GUI 运行 + 1 次 JAR CLI 筛选 + 1 次 DOM 探针
- exit_codes: 驱动全部正常退出（被测应用故障为预期观察对象）
- evidence_paths: `build/p0-baseline/evidence-*.json`（原始证据，不入库）；关键结论已回填 `tests/fixtures/legacy/manifest.json` 与 `tests/fixtures/scripts/contract.md`
- rollback_point: 969b69850fb9970192511a0539cf268d3e7e7c39

## 驱动方法

`build/p0-baseline/drive-gui.mjs`：以真实 Electron 启动旧应用（`--input` + `--remote-debugging-port`），经 CDP `Runtime.evaluate` 读取 Fancytree 树/DOM/日志面板（`conv_data` 为模块作用域不可直接访问，改经 `node.data.item` 活引用提取），模拟点击开始转换/自定义按钮/弹框按钮/ESC，自动处理原生 dialog。注意驱动环境必须清除 `ELECTRON_RUN_AS_NODE=1`（本机 shell 预设了该变量，否则 Electron 以 Node 模式运行不创建窗口）。

## 固定基线组合（供 C15 及后续回归）

- 配置：`build/p0-baseline/legacy-run/convert_list.xml`（3 条目 × 2 输出格式 bin+json；含 set_name、before/after 事件；升级表条目带 `--data-validator-error-version 5 --disable-excel-formular`、嵌套数组条目带 `MacroSource`，对齐上游 `gen_sample_output.ps1` 规范调用）。
- golden 输出已入库：`tests/fixtures/conversion/golden/`（6 个文件 + `SHA256SUMS.txt`）。
- **确定性验证**：同一组合两次运行输出逐字节一致（SHA-256 全等）。
- 事件顺序（日志实证）：`before(run_seq=6, items=3)` → Java 进程 → `after(items=3)` → `All jobs done.`；转换失败时 after 不执行。
- 命令形态（实证，以下代码段省略实际行首的一个前导空格）：全局段 `-p "protobuf" -a "1.0.0.0" --validator-rules custom_validator.yaml -f "proto_v3/kind.pb" -d "."`；任务段 `-t "json" -n "/(?i)\.bin$/\.json/" -o "../output" [-s file -m scheme | 逐键 -m k=v]`；每行 `\r\n` 结尾写入 java stdin；Java 进程 = `java <java_options> -jar <jar> --stdin`，cwd=work_dir。
- 派发顺序实证为 **LIFO**（`pending_script.pop()`，嵌套数组→升级表→人物表逆序）；每个 stdout/stderr data 事件触发下一条派发（BD-03 属实）。
- 失败归并实证：两个进程各失败 2 任务 → 各 exit code 2 → `failed_count=4` → `All jobs done, 4 job(s) failed.`（exit code 累计语义确认）。
- 并发数：实测机器上 GUI 启动日志 `转表并发数: 2`（源码 clamp 到 2）。

## 逐项运行观察（对应 manifest 回填）

| 观察 | 结果 | 证据 |
| --- | --- | --- |
| F01 include 竞态 | 3/3 复现且结果一致：只有第一个 include（child-a）进入顺序链；child-b 的条目**永久丢失**（树已渲染不重建），但 child-b 的全局值（output_type=json、data_version=1.0.0.2）覆盖了父配置值 | evidence-include-parent{,-r2,-r3}.json |
| F01 循环 include | 自引用触发 `alert()` **阻塞页面加载**约 25 s（一次曾 147 s），随后继续，条目只加载一次；CDP 未捕获对话框内容（alert 发生在 CDP 附着前），解除机制未确定 | evidence-include-cycle.json |
| F01 非法 XML | `invalid.xml`（未闭合 item）被 HTML 解析容错**正常加载**，无报错 | evidence-invalid.json |
| F01 脚本文本语义 | `<script>` 为 HTML raw-text 元素，`<>&` 原样保留（官方 sample 的 `delaycall` 含 `left_times > 0` 实测正常运行）；`<set_name>`/事件元素为未知元素，`.html()` 重新转义，`<>&` 导致**编译必然失败**（`Unexpected token ';'`，事件静默缺席只记错误日志）；`&lt;` 不会被解码还原；CDATA 无支持 | evidence-script-text-edge.json、evidence-fault-log-storm.json、evidence-sample-official.json、DOM 探针输出 |
| F02 树模型 | DataSource 回填 file、default_scheme 补全、desc 拼接、ft_node 双向活引用（`ft_node_roundtrip: true`）、分类 key 自动 `_N`、条目数字自增 id | evidence-model-only.json |
| F04 选择器 | `default_selected` 在加载时自动执行；非法 regex 记错误日志并回退为**对原规则串精确匹配**（无选中）；by_sheets regex 命中 DataSource 条目 | evidence-selector-{exact,invalid,official}.json |
| F05/F09 按钮脚本 | 动作链顺序执行；同按钮 `data` 跨脚本共享（`delaycall` 置 `data.running`、`自定义脚本` 读到 `call_times=1`）**运行验证通过** | evidence-sample-official.json |
| F09 事件 data | `on_before_convert` 各事件 `data` **每次新建**：before-2 读到 `data.step=null`（README"全局状态"说法与实现不符，实测确认） | evidence-events-order-convert.json |
| F10 on_append_log | 同条日志多 hook 共享 VM context（hook2 可见 hook1 的 globalThis 标记并改写 module_name）；改写按序累积（`[hook1]…[hook2]`）；hook 内 `log_info` 被递归守卫拦截走原始渲染；**每条日志新建 context**（跨消息无全局状态） | evidence-append-log-convert.json、evidence-fault-log-recursion.json |
| F10 `--log-configure` | 外部 log4js 配置生效（自定义文件 appender 产出日志） | evidence-log-configure.json + custom-log-probe.log |
| F12 java 检测 | `java -version` 解析存在**竞态**：`exit` 事件可能先于 stdout 数据到达 → 偶发误报"检测不到java"（20 次运行中 2 次复现） | evidence-include-cycle.json、evidence-selector-invalid.json 日志 |
| alert_warning | 点"是"顺序：`yes()` → `on_close()`（实测）；**ESC 关闭不触发任何回调**，事件挂起至超时（4 s 超时实测 `Run event callback timeout`） | evidence-alert-yes.json、evidence-alert-esc.json |
| R01 同步 throw | 事件同步抛错 → 记录 `CONV EVENT EXCEPTION` → 链拒绝 → 不启动转换 → `All jobs done, 1 job(s) failed.`，GUI 存活 | evidence-fault-sync-throw.json |
| R01 异步 throw | 定时器回调抛错 → **渲染进程失去响应 ≥60 s**（CDP 调用超时），主进程存活——README 白屏问题实证 | evidence-fault-async-throw.json |
| R02 永不 resolve | 外部 `setTimeout` 兜底生效，2 s 超时拒绝 | evidence-fault-never-resolve.json |
| R02 set_name 死循环 | 渲染进程在加载期完全冻结，CDP 连 `Runtime.enable` 都无响应，主进程存活（set_name 无 VM timeout） | evidence-fault-infinite-loop.json |
| R03 process.exit | 按钮脚本 `process.exit(1)` → 渲染进程死亡、主进程存活（死窗口）；运行后系统残留 2 个孤儿 electron 进程（需手动清理） | evidence-fault-process-exit.json |
| R09 日志风暴 | 10 万条 `log_info`：同步 DOM 追加使 GUI 冻结约 121 s，VM timeout(120 s) 中止脚本（`Script execution timed out after 120000ms`），期间仅写入 16,554/100,000 条（约 137 条/s，瓶颈为 DOM append + scrollTop 同步布局） | evidence-fault-log-storm.json |

## JAR 兼容范围记录

- 2.23.6 + main 样本：三类条目全部可转（需补 MacroSource / validator 选项）。
- 2.14.1 + main 样本：协议初始化失败（`Unknown validator InMacroTable/InValues/Not/InText`）——main 样本需要含这些验证器的较新 JAR。**"支持的 JAR 范围"不能 blanket 承诺**，P3 转换内核需按固定清单验证。
- 官方 sample.xml 默认引用 2.14.0-rc3，其样本数据（同时代）与 main 分支样本数据不同步；本基线以 main 样本 + 2.23.6 为准。

## remaining_limits

- 100k 条目压力、取消/重置后 Java 孤儿进程实证、多配置会话并行未在本轮执行（转 P0-05/P6）。
- include 循环 alert 的解除机制、渲染进程崩溃后主进程窗口的确切状态（仅观察到进程存活）留待 P2 故障原型复核。
- `conv_env_check` 竞态只在部分运行复现，未定位到确切时序窗口。
- D3 真实用户脚本样本仍未收集——所有结论基于官方示例与编制 fixture。
