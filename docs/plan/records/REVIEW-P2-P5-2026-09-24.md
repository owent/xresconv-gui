# 上次审查后的增量 Review（2026-09-24）

[阶段索引](README.md) · [主计划](../../../Plan.md)

## 审查合同

- 基线：`38aee0c`（含上次 Review）至 `d37549a`；任务开始时工作树干净。
- 范围：P2 新增监督/NodeMirror/隔离 matcher/模块兼容，P3 编排扩展，P4-01 至 P4-04a 的 RPC/树/参数后端，P5-01 发行矩阵，以及图标/LFS/构建配置改动。
- 不恢复已暂停的 P4-04b/P4-05+、P5-02+；不提交、不发布。
- 基线验证：454 例 Node/前端测试通过；新增边界必须先写失败回归再修复。
- 修复遵守现有合同：启动/关闭共享完成结果，关闭后不能复活；旧树操作不能写入重新加载的配置；RPC 成功必须表示请求实际被接纳；设置/预览快照不暴露可写内部别名；UI 迟到响应不得覆盖新配置或故障状态；原生句柄不得重复关闭；发行校验诊断不得回显疑似秘密。
- 保持 protocol v1 既有字段和业务语义，优先修复实现；如需扩展/破坏冻结合同，先更新权威说明与生成/漂移守卫。
- 方案：沿壳→guardian→backend→worker 及 UI→树/RPC 逐段检查，用可控 Promise/假进程验证竞争，再复验真实子进程、Rust 壳、WebView2、固定 JAR 和质量门禁。资源隔离按 D4 可信脚本边界，不声明恶意代码防护。
- 回退：从干净起点按本轮 diff 回退对应修复；不删除基线实现。风险集中在原生资源释放、异步取消和旧脚本共享引用，分别以生命周期/差分测试约束。

## 修复与验证

审查输入为 172 个增量文件；实现和验收范围仍止于 P4-04a / P5-01。以下缺陷均以失败回归或真实 E2E 复现，修复后再验证。

| 范围 | 已复现问题与修复 | 回归入口 |
| --- | --- | --- |
| RPC 接纳 | 并发 run 在 await 启动后使用过期预检；终态日志尚未收尾也回成功。启动后再次校验，活动运行结束前禁止新 run/加载/设置/选择操作 | backend `review-incremental.test.ts` |
| 树与脚本 | reload 把版本重置为 1；失败盖章部分生效；字段/展开/option 变更未推进版本；自定义字段在下一镜像丢失。版本跨加载单调递增，先校验整条盖章，完整保留并克隆 item 字段，字段写入不调用原型 setter | backend `review-incremental.test.ts`、既有 NodeMirror/差分测试 |
| 设置与预览 | 可写数组别名污染会话；矩阵解除限制后仍禁选，禁用叶节点时祖先选择状态未重算；空格拼接的冲突分组碰撞。输入/输出与运行覆盖使用独立快照，重估矩阵先释放旧限制，禁用时按旧版级联更新祖先，分组使用二元组编码 | backend `review-incremental.test.ts`、`tree-state.test.ts` |
| matcher | 重复 start 提前完成，排队输入随外部数组变化。复用启动结果并在入队时复制；补充队列上限 128、结果类型/基数检查、旧 worker 回调检查及共享 shutdown/清理结果 | backend `review-matcher.test.ts`、`matcher-service.test.ts` |
| guardian | 重复 start/shutdown 提前完成，关停期间迟到握手可能恢复 ready。共享生命周期结果；关闭即失效旧 slot；失败清理必须等待后才可重启；畸形 health 不再抛出未捕获 TypeError | guardian `review-lifecycle.test.ts`、supervision/relay |
| 原生进程 | Job 句柄重复关闭、终止后登记新进程无效、AssignProcessToJobObject 失败仍声称成功；运行器提前于 scope.dispose 报完成。共享 dispose 结果、单次关闭句柄，拒绝并终止失效登记，报告分配失败，运行器等待 close 与 scope 清理 | guardian `review-process-handles.test.ts`、`review-run-cleanup.test.ts`、真实进程树测试 |
| Rust 壳 | 管道写阻塞不受 RPC deadline 约束；伪角色/协议版本回复可完成请求；shutdown 后 ensure 又启动 guardian。增加有界独立 writer、持久通道死亡状态、入站身份/形状校验、关闭终态；超时后关闭通道且禁止自动重放；窗口关闭的等待移出 UI 线程 | `guardian.rs` 3 个新增回归及既有真实通道测试 |
| 帧预算 | 3,601,398 字节快照超过 1 MiB 后被 send 吞错，调用方只能等超时。返回关联的 `RESPONSE_TOO_LARGE` 错误；出站积压预算 8 MiB，超限关闭通道。错误不表示已接纳的业务操作回滚 | guardian `backend-rpc-relay.test.ts` |
| UI 异步状态 | 旧刷新覆盖新配置/死亡状态；旧配置排队选择写入新树；第二次失败加载丢掉首次结果；失败路径标题与树不一致。按会话代际和请求水位丢弃旧响应，互斥加载，成功后更新配置路径 | desktop `review-async.test.ts` |
| UI 展示 | 深层搜索因仅比较子节点数量保留未匹配后代；长期 guardian stats 被误读成 one-shot NodeHealth 并显示 backend ok。修正子树引用比较，展示实际监督状态/代际并订阅 ready/died 刷新 | desktop 单测与真实 WebView2 |
| 发行校验 | 疑似令牌值被写入错误详情；相对路径可穿越/绝对化；targetTriple 与 OS/架构不一致；矩阵生成漏目标仍成功；runtime manifest 跳过语义校验。秘密诊断脱敏，路径、triple、矩阵集合、运行时策略在入口统一校验 | packaging `review-boundaries.test.ts` |
| E2E | 启动参数进入折叠区后用例只读到标题。先打开详情再断言；显式选择原生 WebDriver 窗口，避免每个命令尝试缺失的可选插件；WDIO 进程及驱动由独立 ProcessScope 收尾 | `tests/desktop/run.mjs`、`launch.spec.mjs` |

既有测试修正两处无效夹具：盖章状态补齐合同要求的 `partsel`；空 scope 终止后不复用来登记另一进程。App mock 改为真实长期 health 形状，配置加载用真实成功响应形状。未跳过失败测试。

## 验证证据

平台：Windows x64，Node v24.21.0 / Yarn 4.18.0，WebView2 与 msedgedriver 153.0.4234.48。工作目录为仓库根。

| 命令 | 结果 |
| --- | --- |
| `corepack yarn test:unit` | 489 例、64 个文件；backend 178、compat 43、contracts 23、desktop 42、guardian 89、ipc 10、packaging 63、script-host 41；相对基线增加 35 例 |
| `corepack yarn typecheck` / `corepack yarn lint` | 通过；修改的来源索引另过 markdownlint |
| `corepack yarn test:shell` | 8 例通过，新增 3 例；测试进程正常加载，无 Tauri/wry 类型进入 guardian 模块 |
| `corepack yarn check:shell` / `cargo fmt --all -- --check` | 通过 |
| `corepack yarn test:desktop` | debug 前端/Tauri 构建成功；真实 WebView2 3/3 通过 |
| `node tests/conversion/run-e2e.mjs` | 八格式、30/30 文件 MATCH；报告 `build/g3-e2e/report.json`（既有索引保留）。UE 清单只归一化输出根路径，沿用 P3-10 合同 |
| `git lfs fsck --objects --pointers` / `git diff --check` | 通过 |

本轮 RED 阶段还包含全量门禁暴露的夹具问题、过期 E2E 折叠区断言，均保留真实失败含义后修复。没有将测试发现数等同于独立缺陷数。

## 仍未验收的范围

- Linux/macOS 实机/CI、POSIX 多层独立进程组组合回收仍未验证；本机 Job Object 测试不能替代这些证据。taskkill 在中间父进程先退出时的既有孤儿进程边界仍适用。
- P4-04b/P4-05+、P5-02+ 仍暂停；未实施设置表单、剩余业务 UI、实体安装包、签名或发布矩阵。
- WDIO 上游仍输出 Node DEP0190（内部 shell spawn）及 session 已删除后的 mock 清理告警；不屏蔽告警，不向正式包添加测试插件。本轮拥有的 driver/app 进程已回收，运行前已有的其他 driver 进程未触碰。
- 真实 JAR 差分沿用 P3-10 的同版本 JAR 自比对口径，不等同于旧 Electron 全流程回归或跨平台发布验收。
- 已同步主计划、分册合同、记录索引、来源索引和测试入口。Agent 规则、Skills、旧 Electron 发布配置无新增接口或规则需要修改；未提交、未发布。
