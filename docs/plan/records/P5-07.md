# P5-07 签名/SBOM/清单（SBOM 与哈希清单就绪；签名阻塞于受控环境）

- task_id: P5-07
- status: partial（SBOM 生成与许可/哈希清单完成；代码签名/公证属受控发行环境，本机不接触证书）
- owner: main agent
- source_commit: 工作树（接续 P5-06）
- os_arch: win32/x64 + WSL linux/x86_64
- evidence_paths: `packages/packaging/src/sbom.ts`（新）、`packages/packaging/test/sbom.test.ts`（新 3 例）、`build/dist/sbom.spdx.json`（真实 manifest 生成样张）、`build/dist/*.sha256`（P5-03/05/06 产物边车）、本文件
- rollback_point: 文件级可恢复

## 已完成

1. **SPDX 2.3 SBOM 生成**（`buildSpdx`/`packageInventory`）：以 runtime-manifest files[] 为唯一事实源聚合（应用/node-dist/npm:pkg@ver），DEPENDS_ON 关系、UNSPECIFIED→NOASSERTION、documentNamespace 由版本+commit 派生；测试 3 例。真实 Windows manifest 样张：**59 包**（`build/dist/sbom.spdx.json`）。
2. **许可与哈希清单**：manifest files[] 含逐文件 sha256/license（P5-02 既有，1323 文件无 UNSPECIFIED）；产物级 SHA-256 边车随 P5-03/05/06 全部产出。
3. **manifest schema/signingEvidence 字段**：P5-01 既有（`signingEvidence` 数组），待受控环境填写。

## 阻塞与移交（受控发行环境）

- Windows Authenticode、macOS codesign/notarization/stapling：需要证书与凭据，本机不获取/不接触密钥（AGENTS 密钥纪律）；签名后的最终介质才计发行 hash（05 册）。
- 完整 SBOM 附件随正式 draft release 汇总（CI-06）。

## 遗留

- npm 包的 license 文本全文收集（当前 manifest 记录声明值；PK08 的 license 完整性验收在 P5-10/CI 复核）。
