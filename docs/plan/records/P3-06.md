# P3-06 编排服务记录（覆盖 P3-03 set_name 整合 / P3-08 取消与状态机 / P3-09 与 P2-08 日志管线 backend 侧）

- task_id: P3-03、P3-08、P3-09、P2-08（backend 侧，合并交付）
- status: done
- owner: BackendService（并行子代理）
- source_commit: 2e8ea87c2d1b30434a8266e907db33777da62bff（+ 本切片工作树改动）
- input_versions: Node v24.21.0、Yarn 4.18.0、typescript 7.0.2、vitest 5.0.1、log4js 6.9.1（yarn.lock 既有精确版本，未新增外部依赖）、OpenJDK 25.0.4.1、xresloader-2.23.7.jar（真实构件冒烟）；新增 workspace 引用：backend→@xresconv/guardian（ScriptWorkerPool/runJavaBatch）
- commands（cwd=D:\workspace\github\xresloader\xresconv-gui，os_arch=win32/x64）:
  - `corepack yarn install --no-immutable` → exit 0（刷新 lockfile：仅追加 backend 对 guardian/log4js 既有版本的引用）
  - `CI=1 corepack yarn install` → exit 0
  - `CI=1 corepack yarn workspace @xresconv/backend typecheck` → exit 0
  - `CI=1 corepack yarn workspace @xresconv/backend test` → exit 0（12 文件 87 例全过，976ms；含真实 worker 进程与真实 JAR 冒烟，JAR 用例单独 verbose 复核 363ms 通过）
- evidence_paths: `packages/backend/src/service/{format,log-pipeline,load-config,run,session}.ts`、`packages/backend/src/index.ts`（追加导出）、`packages/backend/{package.json,tsconfig.json}`、`packages/backend/test/service/{helpers,load-config.test,run.test,append-log.test,log-pipeline.test,smoke-jar.test}.ts`、`packages/backend/test/fixtures/service/*.xml`（9 件）、本文件
- rollback_point: 新增 packages/backend/src/service/**与 test/service/**；package.json 两条依赖、tsconfig 两行、index.ts 追加导出均可由 git 工作树恢复；未改冻结契约与其他包

## 实施结果

- `service/session.ts`：ConversionSession。状态机复用冻结的 domain/run-state.ts（assertTransition/isTerminal），会话是迁移唯一 owner。持有 ParsedConfig、日志管线、run_seq 代际计数；ScriptWorkerPool 与 JavaRunner 由调用方注入（backend 不自己 spawn）。`loadConfig`（idle/ready/终态→loading→ready/解析硬错误→failed 并抛出）、`runConversion`（委托 run.ts）、`cancel`（仅运行中状态有效，幂等，终态 cancelled 恰好一次）、`dispose`（drain + log4js flush 有界 5s，不关共享池）。pool.onLog 路由：log envelope → 管线（在途 on_append_log invocation 产出按 invocation_id bypass，BD-O11）；WorkerDiag → "WORKER" 模块诊断（bypass hook 链）。
- `service/load-config.ts`（P3-03）：parseXmlConfig → 对树文档序每个 item 逐条 invoke set_name（context={work_dir: resolveWorkDir 后/config.dir 兜底， configure_file: set_name 所在文件， item_data}）→ 应用 ops set_fields（value===null 删键）；error outcome 部分修改保留（BD-O16）；invoke 级失败/超时 → "GUI EVENT" error 诊断、item 保留原值、加载继续（main.js:1749-1757）。
- `service/run.ts`（P3-08）：runConversion。before 链（逐 hook 顺序 invoke，reject/超时/异常 → failed_count++ 并中止链，main.js:2301-2392）→ buildConversionPlan → java 并发分片（round-robin，每分片一次 runner 调用）→ 全退后 failed_count==0 才跑 after 链（main.js:2396-2407/2179-2185）→ finally：清 hook 窗口（main.js:2424）、drain、按 failed_count 输出旧文案（main.js:2426-2446）。事件上下文 main.js:2253-2298（global_options={"-p","-a"?}、selected_items 快照、run_seq）。日志：进程启动 module=work_dir、派发 module `[CONV N]`、stdout→notice、stderr 前 5 字符 "[warn" 分 warning/error、退出分级（main.js:2125-2220）。
- `service/log-pipeline.ts`（P2-08 backend 侧 + P3-09）：有界队列（默认 10000，溢出丢最老 + droppedCount + "LOG" 诊断直发，BD-O10）；级别→样式（main.js:245/263/283/304）；text=`[module]: message`；Error 重路由 error（main.js:238-244）；on_append_log 链经尾链串行保序，logObject 贯穿合并（main.js:183-204），hook 异常部分修改保留+剩余跳过（main.js:205-212），仅在 hookRunner 窗口（append_log_context）生效（main.js:2299/2424）。log4js sink：默认消费 src/log4js.json，configurePath 覆盖（--log-configure 对应），失败回退默认并记诊断（main.js:841-859）；级别映射 info/notice→info、warning→warn、error→error（main.js:247-313）；shutdown 有界 flush。
- `src/index.ts` 追加导出；`package.json` 加 `@xresconv/guardian: workspace:*` 与 `log4js: 6.9.1`；`tsconfig.json` module ESNext + moduleResolution bundler（BD-O13）。

## 行为差异清单（BD-O?）

| 编号 | 差异 | 旧版锚点 |
| --- | --- | --- |
| BD-O1 | set_name 硬超时（默认 5000ms，可注入）：旧版 vm 无 options、死循环卡死加载；新版超时由 guardian WORKER_TIMEOUT 销毁并补员 worker，记 "GUI EVENT" 诊断、item 保留原值、加载继续 | main.js:1748、1749-1757 |
| BD-O2 | parallelism 默认 2（保留旧版启动硬压 2 语义）可配，压到 [1,16]；取消旧版 >6 确认弹窗（headless 无 UI，阈值确认归调用方/UI 层） | main.js:6-9、2574-2639 |
| BD-O3 | 任务确定分片选定 round-robin（task i → 分片 i%N）；旧版共享栈 LIFO pop 动态领取（顺序不定） | main.js:2092、2118 |
| BD-O4 | 无结构差异、语义确认：before reject → run_all 与 after 全跳过（promise 拒绝传播）；run_all reject（failed_count>0）→ after 跳过；末尾 catch 记 "CONV" error 后 finally 仍输出 "All jobs done, N job(s) failed." | main.js:2396-2418、2179-2185 |
| BD-O5 | 0 任务不 spawn java（旧版 spawn parallelism 个进程立即关 stdin 退出 0，可观测结果等价） | main.js:2235-2237、2102-2110 |
| BD-O6 | 取消为新硬能力：冻结派发 + AbortSignal（runner 侧 SIGTERM→2s 宽限→SIGKILL）；旧版只有软取消（reset=窗口 reload + run_seq 换代停喂）。会话级 cancel 不杀共享 worker 池，在途 invoke 由其 timeout 兜底 | main.js:2671、53-56、2091、2226 |
| BD-O7 | 事件上下文 selected_nodes 传 []（无 Fancytree；BD-S9 快照降级，NodeMirror 归 P2-05）；selected_items 为 JSON 快照，hook 对快照的修改不跨 hook 共享（旧版共享活数组引用） | main.js:2258-2259、2004-2008 |
| BD-O8 | buildConversionPlan 融合构建与存在性检查且置于 before 链之后（任务书既定顺序；hook 改快照本就不影响计划，顺序差异不可观测）。XRESLOADER_NOT_FOUND 时失败计数近似 = 选中 item 数（旧版 = pending_script 任务行数；构建失败时任务数不可得，取下界） | main.js:2068-2085 |
| BD-O9 | java 进程级失败（SpawnError 等）保守计该分片全部任务失败（修复旧版 B5 缺陷：error 事件不计 failed_count 可能误显成功） | main.js:2156-2176；P0-08 附录 B5 |
| BD-O10 | 内存日志队列有界（默认 10000），溢出丢最老 + droppedCount + 直发 "LOG" 诊断（不进队列防递归驱逐）；旧版无界 DOM 追加。完整日志由 log4js 落盘承担 | main.js:218-228 |
| BD-O11 | on_append_log 递归保护按 invocation_id 判定 bypass（旧版同步 guard 布尔）；hook 内 log_* 产出直接落队且先于被 hook 的日志（与旧版渲染次序一致）。hook 处理异步化后经尾链串行化，保证按到达顺序进链/落队 | main.js:182-214 |
| BD-O12 | log4js 仅在会话显式传 `log4js` 选项时挂载（headless 默认不落 cwd 文件）；配置失败回退默认并记 "LOG" 诊断；shutdown flush 有界 5s | main.js:841-859 |
| BD-O13 | backend tsconfig 改 module ESNext + moduleResolution bundler（与 guardian/script-host 同因：冻结的 contracts 根导出无扩展名 type re-export，NodeNext 消费者 TS2835；P2-03 记录已预告） | docs/plan/records/P2-03.md |
| BD-O14 | 终态后再运行经 terminal→loading→ready 重新武装（冻结状态机不允许 terminal→before_hooks 直达） | domain/run-state.ts TRANSITIONS |
| BD-O15 | 取消终态新增日志 "Conversion cancelled."（旧版无取消能力，无对应文案） | main.js:2239-2448（无对应） |
| BD-O16 | set_name error outcome 应用部分修改 ops（与旧版活引用语义一致）；invoke 级失败（超时等）无 ops，item 保留原值 | main.js:1711、1749-1757；BD-S3 |
| BD-O17 | hook "error" outcome（同步异常）记 "CONV EVENT" error（worker 消息已带 "CONV EVENT EXCEPTION" 前缀）+ failed_count++ 并中止链；worker 级 invoke 失败（WORKER_TIMEOUT 等）同路径（旧版等价场景是渲染进程卡死，属新可观测失败模式） | main.js:2373-2392 |
| BD-O18 | 进程退出日志合并为单条分级 entry（旧版 notice 显示 + logger.error/info 双写 log4js）；任务行不可编码（StdinEncodeError）→ 该任务计失败并跳过（旧版整行直拼无此失败模式） | main.js:2156-2172、1955-2045 |

## 验收对照（逐条指到 P0-08/main.js 锚点）

- [x] set_name：fixture 改名生效（`alpha→alpha-renamed`，树节点同对象可见）；异常脚本记 "GUI EVENT" 诊断且加载继续、部分修改保留（main.js:1749-1757/1711）；while(true) 注入 300ms 超时 → 诊断不卡死、item 保留原名（BD-O1）
- [x] runConversion 全链：真实 worker + fake runner，日志次序 BEFORE→派发 `[CONV 1]`→AFTER→"All jobs done." 有断言（main.js:2301-2448）；状态序列 loading→ready→before_hooks→converting→after_hooks→succeeded
- [x] before reject → java/after 全跳过、failed_count=1、"stop-now" 记 "CONV"、失败文案（main.js:2315、2396-2418、2426-2437）
- [x] 退出码累加 failed_count（fake runner exitCode=2 → "All jobs done, 2 job(s) failed."，after 跳过；main.js:2175-2176/2179-2185）
- [x] 确定分片 round-robin：5 任务并发 2 → [0,2,4]/[1,3]（BD-O3）
- [x] cancel：运行中（fake runner 挂起）cancel → 终态 cancelled 恰好一次（重复 cancel no-op）、runner 收到 AbortSignal、无 "All jobs done"、有 "Conversion cancelled."（BD-O6/BD-O15）
- [x] 状态机违规：未加载配置启动/运行中再启动均抛错（复用 assertTransition 语义）
- [x] on_append_log：两 hook 链式改写（"[H1] "+大写 → "[H1] CHAIN-TARGET"）；窗口外日志不进链（main.js:2424）；hook 内 log_info 不递归且先于被 hook 日志落队（main.js:182-214）；异常 hook 部分修改保留、剩余跳过、"APPEND LOG EVENT EXCEPTION" 记账（main.js:205-212）
- [x] 有界队列：容量 3 溢出 → droppedCount=2 + 2 条 "LOG" 诊断、队列只留最新 3 条（BD-O10）
- [x] log4js：tmpdir 文件 appender 真实落盘（shutdown 前 flush，级别 WARN 可见）；配置失败回退默认并记诊断（main.js:841-859）
- [x] 真实 JAR 冒烟：official 路径 JAR + sample 树单任务 const 转储跑通（产物 kind_const.lua 非空、"[Process 1 exit.]"、"All jobs done."；构件缺失时 skipIf）
- [x] 真实进程测试全部显式超时（vitest timeout 30s/冒烟 180s；内部等待 waitUntil 有界 10s）；无协议 mock（pool 真实 spawn script-host worker）
- [x] 验收命令链全绿：`CI=1 corepack yarn install` exit 0、`typecheck` exit 0、`test` exit 0（12 文件 87 例）

### EX/SC/CF 差距声明

- EX（转换执行）：hook 顺序/取消/退出码/收尾文案均有行为断言并指到行号。[!] 差距：并发分片与派发日志用 fake runner 断言（不 spawn java），真实 java 路径仅冒烟单任务（单进程）；多分片真实 JVM 并发未实测。
- SC（脚本上下文）：set_name/事件/append_log 的上下文键与 ops 语义经真实 worker 端到端验证。[!] 差距：selected_nodes 恒 []（BD-O7）；按钮入口不在本切片（P2-05 范围）。
- CF（配置面）：复用 P3-01 loader；本切片只新增 service fixtures（9 件），未扩充解析面覆盖。

## remaining_limits

- 日志虚拟列表/渲染归 P4 UI（本层只保证有界队列 + subscribe/sink）；队列 backpressure 上限实测归 P6。
- selected_nodes/ft_node 镜像归 P2-05（BD-O7）；按钮脚本编排（data 跨点击）不在本切片。
- 多分片真实 JVM 并发、UNC/非 BMP 路径真实执行未覆盖（编码器单测已有 UNC token 往返，P3-05）。
- set_name 的 work_dir 取合并后配置的解析值（旧版按"当前处理文件"逐文件快照；BD-C13 语义下生效 gui 恒属入口文件，差异仅当 include 文件曾定义 gui 且 work_dir 逐文件不同时可观测——该场景旧版竞态不定，见 BD-C1）。
- cancel() 对"worker 在途 invoke"不抢先中止（共享池语义，BD-O6）；若 hook 自身死循环，run 的取消收尾要等 guardian 超时销毁（有界）。
- pool.onLog/onDialogRequest 为单订阅通道：会话直接赋值；多方订阅需调用方自行合流（当前唯一消费者即会话）。
