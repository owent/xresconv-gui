# P7 移除旧架构与交接（用户指示：实机验收排最后，功能全部完成）

- task_id: P7
- status: done（代码删除与文档交接；draft release 构建随 P5-10 实机验收后执行）
- owner: main agent
- source_commit: 73410d5..5977f7a（dev）
- os_arch: win32/x64
- evidence_paths: 删除清单见下；`packages/backend/src/service/log-sink.ts`（默认配置内联）、`packages/backend/bin/service.mjs` + `packages/guardian/bin/service.mjs` + `src-tauri/src/{lib,guardian}.rs`（--log-configure 接线）、`README.md`/`AGENTS.md`/`docs/ai/source-index.md`（交接）、本文件
- rollback_point: 旧稳定发行保留在 [v2.6.0 tag](https://github.com/owent/xresconv-gui/releases/tag/v2.6.0)；代码回滚=git revert 本提交序列

## 1. 删除清单（旧 Electron 架构）

| 删除项 | 说明 |
| --- | --- |
| `src/`（setup.js/main.js/index.html/main.css/log4js.json） | 旧主进程+渲染器 |
| `gulpfile.js`、`scripts/patch-fancytree.js` | 旧构建管线与 fancytree 修补 |
| `.github/workflows/build.yml` | 旧三平台 Electron 构建（新 CI=ci.yml/release.yml/stale.yml） |
| 根 package.json：electron/@electron/packager/gulp/jquery/jquery.fancytree/bootstrap/@popperjs 依赖 | 保留 adm-zip/compressing/log4js/minimatch（新架构用户脚本供给包） |
| 根 package.json：prepare 钩子与 start/debug/package-* 脚本 | 新入口=dev:desktop/build:desktop/package:{windows,linux,macos} |

**随删除暴露并修复的真实缺陷**：`log-sink.ts` 默认配置读 `src/log4js.json`（旧架构文件）——删除后 5 例测试失败。修复=默认配置**内联为对象**（发行 bundle 中相对 import.meta.url 的文件查找本就指向不存在的路径，属 P5 遗留隐患，一并消除）；worker 侧同步删除"二次 configure fallback"（配置失败现在如实报 error，不静默降级）。

## 2. 补齐功能缺口：--log-configure（F10/F11）

调研发现 `--log-configure` 仅在 tauri.conf.json CLI 声明，从未接到 backend（`--log-configure` → 无消费者）。本切片完成全链：

```text
壳 CLI 解析(lib.rs setup, let-chains) → set_guardian_log_configure(OnceLock)
→ guardian spawn env XRESCONV_LOG_CONFIGURE(guardian.rs)
→ guardian bin backendEnv 接力(service.mjs)
→ backend bin → BackendRpcApp({log4jsConfigurePath})
→ ConversionSession({log4js:{configurePath}}) → 独立 log4js sink 进程
```

- 回归：log-pipeline 新增"默认配置内联可用"用例（诊断必须为 null）；shell 13 例全绿（含 let-chains 编译）。

## 3. 布局恢复与真实项目验证（用户 2026-09-26 指示，先于删除执行）

- **布局对照旧版 v2.6.0 index.html 重构**：左"转换列表"树（事件开关 footer）｜右侧=文件行（转换列表文件/路径/重载/详细配置开关/并发数）+ 详细配置**默认折叠**（六字段+条目详情+输出矩阵收纳于此）+ 常显快捷行（重命名预设下拉[旧版 5 预设逐字恢复]/协议类型/输出类型[旧版 8 选项文案]）+ **大日志（flex:1）** + 按钮组（全部选中/全部取消/全部展开/全部收起/预览/开始转换/取消/重置——旧版顺序+新增能力）+ 自定义按钮。
- **真实项目端到端验证**（`atsf4g-co/build_jobs_cmake_tools/_generated/xml/xresconv.xml`，用户指定）：加载 119ms（8 分类/21 节点，include 合并）→ 全选 14 条 → 预览 28 任务/2 组冲突（bin+json 双输出矩阵）→ **真实转换 28 任务 0 失败 0.9s**（Java 25 + xresloader-2.23.7.jar）→ 28 个输出文件，`.bytes` 与项目现有发布目录**字节一致**（cmp 通过）。证据脚本 `build/real-config/verify.mts`（临时，已清理出仓）。

## 4. 文档交接

- **README.md**：徽章/简介改 Tauri 2；"已知问题"改为脚本隔离已修复；开发/调试/打包/迁移回滚章节全部按新架构重写；旧 NPM 加速保留。
- **AGENTS.md**：项目定位、技术栈、命令、目录结构、约束（删除 asar=false 等旧条目）按新架构改写。
- **CLAUDE.md**：无需改（导入 AGENTS.md 单一事实源）。
- **docs/ai/source-index.md**：构建/打包行与图标行去除 Electron 引用。
- 回滚入口：README「迁移与回滚」+ v2.6.0 tag。

## 5. 验证

```text
corepack yarn lint / typecheck        # 0 error
corepack yarn test:unit               # 603 passed + 10 跳过（backend 212 含新增内联用例）
corepack yarn test:browser            # 15 passed（三引擎，新布局）
corepack yarn check:shell / test:shell # fmt/clippy 通过；13 例
真实项目转换                            # 28 任务 0 失败，bytes 与现有发布一致
```

## 遗留（如实登记）

- draft release 完整构建与产物复核：随 P5-10 实机验收（用户指示排最后）。
- macOS 实构、签名实跑、VM 安装矩阵、G5/G6 签字式验收：同上。
- P4-06 布局反馈轮引入的"次要面板 details 折叠"已被本轮旧版布局重构取代（折叠能力保留在详细配置区内）。
