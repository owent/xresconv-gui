# P1-08 原生桌面 E2E 最小测试构建（Windows）

- task_id: P1-08
- status: done（Windows）；Linux/macOS 待 CI/实机
- owner: kimi-code
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: tauri-driver（cargo 安装）、msedgedriver 153.0.4234.48（匹配本机 WebView2 运行时 153.0.4234.48）、@wdio/* 9.32.0、@wdio/tauri-service 1.4.0、webdriverio 9.32.0
- commands: `yarn tauri build --debug --no-bundle`、`MSEDGEDRIVER_PATH=build/tools/msedgedriver.exe XRESCONV_E2E_SKIP_BUILD=1 node tests/desktop/run.mjs`
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- case_count: 3（窗口标题、真实 WebView 握手链、CLI 参数可见）
- exit_codes: 0（两轮：构建后首轮、run.mjs 修复后复跑各一次，均 3/3 通过，约 90s/轮）
- evidence_paths: `tests/desktop/run.mjs`、`tests/desktop/wdio.conf.mjs`、`tests/desktop/specs/launch.spec.mjs`、本文件
- rollback_point: 删除 tests/desktop 即回退；不影响其他包

## 实施结果

- 前置：本机已有 tauri-driver；msedgedriver 按 WebView2 运行时版本（153.0.4234.48）下载到 `build/tools/`（gitignored，不进仓库），经 `MSEDGEDRIVER_PATH` 注入 PATH（run.mjs 既有逻辑）。
- 真实应用链路全部通过：tauri-driver 拉起 debug 构建的 `xresconv-gui.exe`（WebView2 153）→ UI `invoke("get_backend_health")` → 壳 spawn guardian（15s 截止）→ guardian spawn backend（5s 截止）→ 合并握手行渲染为 `guardian ok · node v24.21.0 · pid … · backend ok · pid … · protocol v1`。
- `run.mjs` 修复 DEP0190：Windows 下以 `.cmd` 扩展名解析 shim 替代 `shell: true` 拼接参数；复跑确认警告消除且结果不变。
- 观察：tauri-service 的 `afterSession`/`restoreAllMocks` 在会话清理期打印过一段异步调用栈但不影响结果（两轮均 3 passing）；后续启用 IPC mock 时复核。

## 验收对照

- [x] Windows 真实应用握手通过（窗口、CLI 参数、壳→guardian→backend）
- [ ] Linux/macOS 真实应用握手：本机不可验，留 CI/实机（G1 审阅记录，见 P1-09）
- [x] 测试插件不进入正式包：`e2e` feature 独立于默认构建（src-tauri/Cargo.toml），`--debug --no-bundle` 构建即含驱动入口；发行包检查属 P5/P6

## remaining_limits

- 仅 win32/x64；三平台矩阵属 CI 与 P6。
- E2E 驱动与 WebView2 运行时版本需逐一匹配；CI 需固定或自动对齐两者版本（P5/CI-批次）。
