# P1-07 质量入口：Biome / 类型检查 / Vitest / 壳原生检查

- task_id: P1-07
- status: done
- owner: kimi-code
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: Biome 2.5.14、TypeScript 7.0.2、Vitest 5.0.1、markdownlint-cli 0.49.1、cargo/clippy/rustfmt 1.98.1
- commands: `yarn lint`、`yarn typecheck`、`yarn test:unit`、`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --locked -- -D warnings`、`cargo test --workspace --locked`
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `biome.json`、各 workspace `tsconfig.json`、`docs/ai/source-index.md`
- case_count: 32（JS/TS 单元+契约）+ 3（桌面 E2E，P1-08）
- exit_codes: 上述命令全部 0

## 实施结果与本轮修复

- `yarn lint` = `biome check apps packages` + `markdownlint "docs/plan/**/*.md" "Plan.md"`，通过。
- 本轮修复：`ajv/dist/2020` 深导入在 guardian 的 NodeNext 解析下缺扩展名（ajv 8 无 exports map，ESM/NodeNext 必须写 `ajv/dist/2020.js`）；两处 import 排序由 `biome check --write` 自动修复。
- `yarn typecheck`（workspaces foreach）：backend（bundler）/guardian（NodeNext）/contracts/desktop 全绿；NodeNext 与 bundler 两种解析策略并存是刻意的——guardian/backend 的 bin 与测试直接跑在 Node 上，desktop 经 Vite 打包。
- 壳原生检查：fmt/clippy/test 全绿（壳内 0 单测为预期，壳行为由桌面 E2E 覆盖，见 P1-00）。
- 来源索引已更新：测试/lint 行收敛为 P1 已验收状态；sidecar 行补 dev 模式定位事实；桌面自动化行登记 Windows 最小 E2E 实测与驱动版本匹配要求；已知缺口段移除"P1-00 待执行"。

## 新发现（并入规则）

- ajv 8 无 `exports` 字段：NodeNext workspace 深导入必须带 `.js` 扩展名；bundler workspace（desktop/contracts）两种写法都可。新增 Node 侧 workspace 时按 guardian 的 NodeNext 模板。

## remaining_limits

- Biome `files.includes` 覆盖 apps/packages/scripts；`tests/desktop` 的 .mjs 暂未纳入 lint 范围（P4 扩充 E2E 时一并纳入）。
