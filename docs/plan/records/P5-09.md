# P5-09 大小分解、产物扫描与 CI 工作流（含 P5-08/P5-10 状态）

- task_id: P5-09
- status: done（本机可验证范围；G5 全矩阵验收=P5-10 阻塞于实机/CI）
- owner: main agent
- source_commit: 工作树（接续 P5-07）
- os_arch: win32/x64 + WSL linux/x86_64
- evidence_paths: `packages/packaging/src/sbom.ts`（sizeBreakdown）、`build/p5-09/sizereport.ts`（报告入口）、`apps/desktop/test/deps-scan.test.ts`（P4-09 已建）、`.github/workflows/{ci,release,stale}.yml`（重写/新增）、本文件
- rollback_point: 文件级可恢复

## 1. 大小分解（真实 manifest，2026-09-25）

| 目标 | 产物 | 大小 | 组成 |
| --- | --- | --- | --- |
| win x64 bootstrap | NSIS exe | 29 MiB | 应用负载 98.1 MiB（node-dist 89.5 + 三角色 bundle ≈0.9 + npm 闭包 8.6/1311 文件）压缩后 + 引导器 ~2 MiB |
| win x64 offline | NSIS exe | 232 MiB | 同上 + 微软 WebView2 离线安装器 203 MiB（原版未篡改） |
| linux debian-13 x86_64 bootstrap | deb | ~50 MiB 量级（WSL 侧） | 同负载 + Depends: webkit2gtk4.1/gtk3（系统提供） |
| linux x86_64 offline（自含原型） | AppImage | 141 MiB | 负载 + WebKitGTK/GTK/JSC 及传递依赖闭包 |

对照（主计划 §11.5 候选目标：Windows x64 bootstrap 较旧版减少 ≥50%）：旧 v2.6.0 Electron 97.14 MiB → **29 MiB（-70%）**，达标；offline 按口径单列不比百分比。未删除任何业务功能/脚本依赖/许可文件达标（PK07/deps-scan 佐证）。

## 2. 产物扫描（PK08 本机部分）

- 无 jQuery/Bootstrap/Fancytree/Popper 运行依赖（deps-scan 3 例，P4-09）。
- 无测试服务器/测试端口/任意命令执行入口：生产包不含 vitest/typescript（闭包断言）；WDIO embedded provider 未使用（external tauri-driver）；`export_text_file` 为唯一受控写盘命令（lib.rs 单测）。
- 产物哈希/许可：manifest files[] 全量 + SHA-256 边车（P5-07）。

## 3. CI 工作流（CI-01..CI-07 落地，推送后验证）

全部 Action 固定完整 commit SHA（版本注释），SHA 经 GitHub commits API 核验（2026-09-25）：

| Action | 版本 | SHA（前 12） |
| --- | --- | --- |
| actions/checkout | v7.0.1 | 3d3c42e5aac5 |
| actions/setup-node | v7.0.0 | 820762786026 |
| actions/stale | v11.0.0 | 4391f3da665f |
| actions/upload-artifact | v7.0.1 | 043fb46d1a93 |
| actions/download-artifact | v8.0.1 | 3e5f45b2cfb9 |
| actions/cache | v6.1.0 | 55cc8345863c（ci.yml 未用，备用） |
| Swatinem/rust-cache | v2.9.2 | 6323deb102c3 |
| tauri-apps/tauri-action | action-v1.0.0 | 1deb371b0cd8（未采用：矩阵 job 不各自发布，备用） |
| xresloader/upload-to-github-release | v1.6.2 | 7c5757a90c0b |

- **ci.yml（新）**：quality-node（lint/typecheck/unit/contracts/browser 三引擎）+ quality-shell（fmt/clippy/test --locked）+ desktop-e2e 三平台矩阵（tauri-driver + Windows msedgedriver 自动匹配下载，失败证据 artifact）；Node 固定 24 + corepack（不用 setup-node npm cache——P0 缺陷不复活）；并发组按 ref。
- **release.yml（重写）**：v3* 标签触发；windows x64 双变体 / linux ubuntu 双代双变体 / macOS 双架构双变体矩阵；**aggregate 是唯一写 release 的 job**（产物集合与 buildMatrix 精确 diff，不等即失败——fail-closed）；draft、不 overwrite；签名关闭（P5-07 受控环境）。
- **stale.yml**：v11 SHA；90 天/豁免标签语义保留；`days-before-issue-close: -1`（只标记不自动关闭）。
- 旧 build.yml（旧架构 main 分支）保留至 P7 删除。

**验证边界**：workflow 语法为手写未跑 actionlint；推送后首跑验证（与 P1 CI 任务同口径，未推送不声称通过）。

## 4. P5-08/P5-10 状态（如实登记）

- **P5-08（升级/修复/卸载）**：blocked 于 VM/实机——NSIS installMode=both 已配、卸载器随 NSIS 自带、共享 WebView2 不随卸载移除（微软安装器语义）；I08/I09 场景需干净快照与旧版安装（本机不做破坏性安装）。
- **P5-10（G5 全矩阵）**：blocked 于 CI/实机——本机已有 Windows x64 双变体 + WSL Debian 13 双变体实构证据；缺 ARM64/macOS/其余发行版/干净环境安装路径。

## 验证命令

```text
corepack yarn lint / typecheck / test:unit / check:shell / test:shell / test:browser  # 全绿（见 P5-05/06 时点）
node --experimental-strip-types build/p5-09/sizereport.ts                              # 大小分解+SBOM 样张
```
