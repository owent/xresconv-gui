# P1-05 资源路径、图标、CLI 参数入口

- task_id: P1-05
- status: done
- owner: kimi-code
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: Vite 8.3.0、Tauri CLI 2.11.5、tauri.conf v2 schema
- commands: `yarn workspace @xresconv/desktop build`、`yarn tauri build --debug --no-bundle`、guardian 含空格/中文路径用例（P1-04 记录）
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `apps/desktop/vite.config.ts`、`apps/desktop/dist/index.html`、`src-tauri/icons/`、`src-tauri/tauri.conf.json`、`src-tauri/capabilities/default.json`
- linked_test_cases: C14（启动参数）、I09（空格/中文目录，Node 链部分）
- rollback_point: vite base 单行改动可回退

## 实施结果

- `vite.config.ts` 设 `base: "./"`：`dist/index.html` 实际产出 `./assets/index-*.js|.css` 相对引用，生产包经 Tauri 自定义协议加载，不依赖任何 HTTP 服务。
- 图标 `src-tauri/icons/icon.ico|icon.png|icon.icns` 存在并被 `tauri.conf.json bundle.icon` 引用；debug 构建通过（bundle 阶段图标/签名矩阵属 P5）。
- CLI 参数入口：`--input`/`--debug-mode`/`--custom-selector`（多值）/`--custom-button`（多值）/`--log-configure` 在 tauri.conf.json plugins.cli 声明，经 `get_cli_matches` 只读暴露给 UI；桌面 E2E 已断言 UI 可见 `input` 参数（P1-08 记录）。
- 含空格/中文目录：guardian→backend 入口在 `安装 dir with spaces` 下复制运行的用例通过（P1-04 记录），证明 spawn/参数传递/相对入口解析不被空格与非 ASCII 破坏。
- 能力面收敛：`capabilities/default.json` 仅 core/dialog/opener/cli default，无 shell/任意命令权限。

## remaining_limits

- 打包安装目录（resource_dir）的资源定位与随包 Node/JS 条目属 P5；当前为开发模式 exe 相对回溯 + env 覆盖，未宣称安装形态验收。
- MSI/NSIS 产物与图标的打包期校验属 P5。
