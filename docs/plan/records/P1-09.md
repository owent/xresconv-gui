# P1-09 G1 审阅：工具链与骨架出口

- task_id: P1-09
- status: done（审阅结论：G1 条件通过，缺口见下）
- owner: kimi-code
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: Node v24.21.0、Yarn 4.18.0、Rust 1.98.1、Tauri 2.11.6、WebView2 153.0.4234.48
- commands: `yarn install --immutable`、`yarn lint`、`yarn typecheck`、`yarn test:unit`、`yarn test:contracts`、`cargo fmt/clippy/test --locked`、`yarn tauri build --debug --no-bundle`、`node tests/desktop/run.mjs`（详见 P1-01～P1-08 记录）
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64（本机）；Linux/macOS 缺口见下
- evidence_paths: P1-00～P1-08 各记录、`scripts/check-toolchain.mjs`
- rollback_point: 各任务记录自带回退点；G1 审阅不改动代码

## G1 门槛逐项核对

门槛定义（docs/plan/README.md）：**锁定依赖、三平台构建/启动、契约往返**；不得以"只生成脚手架"代替。

| 项 | 结论 | 证据 |
| --- | --- | --- |
| 依赖锁定可复现 | 通过 | 唯一 JS 锁 `yarn.lock`，`--immutable` 通过；`Cargo.lock` 收敛为薄壳；`rust-toolchain.toml`、`packageManager`、`.yarnrc.yml` 固定；`check-toolchain.mjs` 非零即败（P1-01/P1-02） |
| 契约往返 | 通过 | schema 唯一源 → 生成 TS + Ajv 运行时校验同源；壳→guardian→backend 合并握手在真实 WebView2 中通过（P1-04/P1-06/P1-08） |
| 质量入口 | 通过 | lint/typecheck/test:unit（32 例）/cargo fmt+clippy+test 全绿（P1-07） |
| 旧架构可回退对照 | 通过 | Electron 44 基线回归 golden 6/6（P1-03）；新旧产物路径隔离 |
| 三平台构建/启动 | **未通过（缺口）** | 仅 win32/x64 实测；Linux/macOS 无本机手段 |
| 不依赖开发机全局 npm 包 | 通过 | 全部经 corepack yarn workspace 解析；tauri-driver 为 cargo 全局安装但仅测试驱动、不进产物；msedgedriver 在 gitignored `build/tools/` |

## G1 结论

**条件通过**：除"三平台构建/启动"外全部门槛在本机满足并有证据。该缺口的唯一诚实关闭方式是 CI-01～CI-03（05 分册）的三平台 runner 或实机验证；CI 工作流需要提交并推送后才能在 GitHub 运行，本任务按项目规则不创建提交，故记为阻塞项而非静默跳过。

## 阻塞与下一步（交给 P5 前置 / 用户决策）

1. 授权提交并推送当前工作树（或指定分支策略），随后实施 CI-01（validate-toolchain）/CI-02（quality）/CI-03（desktop 三平台 E2E）；Linux runner 需补 WebKitGTK 依赖安装步骤，macOS runner 自带 WKWebView。
2. CI-03 在 Linux/macOS 跑通 launch.spec 同等用例后，G1 缺口关闭，Plan.md P1"三平台窗口/对话框/握手"项方可勾选。
3. 发行冻结前按主计划 §3.2 复查一次版本快照（含 Node 26 Current vs 24 LTS 的最终取舍）。

## 不代表已验收

- P2 脚本隔离、P3 转换内核、P4 完整 UI、P5 安装器、P6 全量验收均未开始；骨架握手不证明任何业务能力。
