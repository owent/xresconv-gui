# P1-02 Yarn 4 迁移（清理锁文件冲突）

- task_id: P1-02
- status: done
- owner: kilo-agent
- source_commit: 969b69850fb9970192511a0539cf268d3e7e7c39（+ 工作树改动）
- input_versions: Yarn 4.18.0（corepack）、Node v24.21.0
- commands: `corepack yarn install`（迁移模式）、`corepack yarn install --immutable`、删除 `package-lock.json`/`pnpm-lock.yaml`
- cwd: D:\workspace\github\xresloader\xresconv-gui
- os_arch: win32/x64
- evidence_paths: `yarn.lock`、`package.json`、`.gitignore`、本文件
- rollback_point: 迁移前三锁文件可由 git 历史恢复

## 实施结果

- `corepack yarn install` 成功将旧 yarn.lock（v1 格式）迁移为 Yarn 4 lockfile；`yarn install --immutable` 通过。
- 按 P1-02 要求删除 `package-lock.json`、`pnpm-lock.yaml`；仓库现在唯一锁文件为 `yarn.lock`（D6 同样适用：Node 侧全部经 Yarn）。
- `.gitignore` 补充 `build/`（证据目录）与 `.yarn/*`（Yarn 4 缓存/安装产物）。
- 修复 `package.json` 既有笔误 `files: ["doc"]`/`directories.doc` → `docs`（否则 `yarn pack` 无法解析目录字段）。
- 迁移后 `devDependencies` 被按字母序重排为 `scripts` 之后的尾段；内容为升级前快照 + 新增包（见 P1-03 / P1-06 / P1-07 记录）。

## 验收对照

- [x] `yarn.lock` 唯一生效；npm/pnpm 锁文件已删除
- [x] `yarn install --immutable` 通过（exit 0，含 prepare 钩子 `patch-fancytree.js` + `gulp copy-libs`）

## 观察

- Yarn 4 在非交互 shell 下偶发 `onCancel` 崩溃一次，重跑通过；记录于 P1-00。
- prepare 钩子（旧 GUI 依赖 `gulp copy-libs`）在新工作区拓扑下仍正常执行。

## remaining_limits

- 无。
